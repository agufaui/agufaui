import type { Theme } from "./types";
import { abutton } from "./button/theme";
import { aalert } from "./alert/theme";
import { aalertError } from "./alertError/theme";

export const defaultTheme: Theme = {
  abutton,
  aalert,
  aalertError,
};
